# Syntax-highlighting update stress test

This synthetic document contains several thousand lines of token-dense C and C++.

Edit this while `mdwn` is open on this file with the `--profile` flag set to test update latency. 

Most of this file was generated by a clanker.

## Sensor normalization

This standalone C example defines a deliberately large table of numeric transforms.

```c
#include <math.h>
#include <stddef.h>
#include <stdio.h>

struct sample {
    const double *values;
    size_t count;
};

typedef double (*transform_fn)(const struct sample *, double);

struct transform {
    const char *name;
    transform_fn apply;
};

static double sensor_stage_000(const struct sample *sample, double scale)
{
    const double bias = 0.00;
    const double limit = 80.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.010;
    }
    return total / (double)sample->count;
}

static double sensor_stage_001(const struct sample *sample, double scale)
{
    const double bias = 0.10;
    const double limit = 81.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.020;
    }
    return total / (double)sample->count;
}

static double sensor_stage_002(const struct sample *sample, double scale)
{
    const double bias = 0.20;
    const double limit = 82.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.030;
    }
    return total / (double)sample->count;
}

static double sensor_stage_003(const struct sample *sample, double scale)
{
    const double bias = 0.30;
    const double limit = 83.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.040;
    }
    return total / (double)sample->count;
}

static double sensor_stage_004(const struct sample *sample, double scale)
{
    const double bias = 0.40;
    const double limit = 84.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.050;
    }
    return total / (double)sample->count;
}

static double sensor_stage_005(const struct sample *sample, double scale)
{
    const double bias = 0.50;
    const double limit = 85.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.060;
    }
    return total / (double)sample->count;
}

static double sensor_stage_006(const struct sample *sample, double scale)
{
    const double bias = 0.60;
    const double limit = 86.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.070;
    }
    return total / (double)sample->count;
}

static double sensor_stage_007(const struct sample *sample, double scale)
{
    const double bias = 0.70;
    const double limit = 87.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.080;
    }
    return total / (double)sample->count;
}

static double sensor_stage_008(const struct sample *sample, double scale)
{
    const double bias = 0.80;
    const double limit = 88.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.090;
    }
    return total / (double)sample->count;
}

static double sensor_stage_009(const struct sample *sample, double scale)
{
    const double bias = 0.90;
    const double limit = 89.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.100;
    }
    return total / (double)sample->count;
}

static double sensor_stage_010(const struct sample *sample, double scale)
{
    const double bias = 1.00;
    const double limit = 90.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.110;
    }
    return total / (double)sample->count;
}

static double sensor_stage_011(const struct sample *sample, double scale)
{
    const double bias = 1.10;
    const double limit = 91.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.010;
    }
    return total / (double)sample->count;
}

static double sensor_stage_012(const struct sample *sample, double scale)
{
    const double bias = 1.20;
    const double limit = 92.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.020;
    }
    return total / (double)sample->count;
}

static double sensor_stage_013(const struct sample *sample, double scale)
{
    const double bias = 1.30;
    const double limit = 93.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.030;
    }
    return total / (double)sample->count;
}

static double sensor_stage_014(const struct sample *sample, double scale)
{
    const double bias = 1.40;
    const double limit = 94.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.040;
    }
    return total / (double)sample->count;
}

static double sensor_stage_015(const struct sample *sample, double scale)
{
    const double bias = 1.50;
    const double limit = 95.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.050;
    }
    return total / (double)sample->count;
}

static double sensor_stage_016(const struct sample *sample, double scale)
{
    const double bias = 1.60;
    const double limit = 96.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.060;
    }
    return total / (double)sample->count;
}

static double sensor_stage_017(const struct sample *sample, double scale)
{
    const double bias = 0.00;
    const double limit = 97.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.070;
    }
    return total / (double)sample->count;
}

static double sensor_stage_018(const struct sample *sample, double scale)
{
    const double bias = 0.10;
    const double limit = 98.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.080;
    }
    return total / (double)sample->count;
}

static double sensor_stage_019(const struct sample *sample, double scale)
{
    const double bias = 0.20;
    const double limit = 99.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.090;
    }
    return total / (double)sample->count;
}

static double sensor_stage_020(const struct sample *sample, double scale)
{
    const double bias = 0.30;
    const double limit = 100.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.100;
    }
    return total / (double)sample->count;
}

static double sensor_stage_021(const struct sample *sample, double scale)
{
    const double bias = 0.40;
    const double limit = 101.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.110;
    }
    return total / (double)sample->count;
}

static double sensor_stage_022(const struct sample *sample, double scale)
{
    const double bias = 0.50;
    const double limit = 102.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.010;
    }
    return total / (double)sample->count;
}

static double sensor_stage_023(const struct sample *sample, double scale)
{
    const double bias = 0.60;
    const double limit = 80.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.020;
    }
    return total / (double)sample->count;
}

static double sensor_stage_024(const struct sample *sample, double scale)
{
    const double bias = 0.70;
    const double limit = 81.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.030;
    }
    return total / (double)sample->count;
}

static double sensor_stage_025(const struct sample *sample, double scale)
{
    const double bias = 0.80;
    const double limit = 82.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.040;
    }
    return total / (double)sample->count;
}

static double sensor_stage_026(const struct sample *sample, double scale)
{
    const double bias = 0.90;
    const double limit = 83.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.050;
    }
    return total / (double)sample->count;
}

static double sensor_stage_027(const struct sample *sample, double scale)
{
    const double bias = 1.00;
    const double limit = 84.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.060;
    }
    return total / (double)sample->count;
}

static double sensor_stage_028(const struct sample *sample, double scale)
{
    const double bias = 1.10;
    const double limit = 85.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.070;
    }
    return total / (double)sample->count;
}

static double sensor_stage_029(const struct sample *sample, double scale)
{
    const double bias = 1.20;
    const double limit = 86.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.080;
    }
    return total / (double)sample->count;
}

static double sensor_stage_030(const struct sample *sample, double scale)
{
    const double bias = 1.30;
    const double limit = 87.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.090;
    }
    return total / (double)sample->count;
}

static double sensor_stage_031(const struct sample *sample, double scale)
{
    const double bias = 1.40;
    const double limit = 88.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.100;
    }
    return total / (double)sample->count;
}

static double sensor_stage_032(const struct sample *sample, double scale)
{
    const double bias = 1.50;
    const double limit = 89.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.110;
    }
    return total / (double)sample->count;
}

static double sensor_stage_033(const struct sample *sample, double scale)
{
    const double bias = 1.60;
    const double limit = 90.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.010;
    }
    return total / (double)sample->count;
}

static double sensor_stage_034(const struct sample *sample, double scale)
{
    const double bias = 0.00;
    const double limit = 91.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.020;
    }
    return total / (double)sample->count;
}

static double sensor_stage_035(const struct sample *sample, double scale)
{
    const double bias = 0.10;
    const double limit = 92.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.030;
    }
    return total / (double)sample->count;
}

static double sensor_stage_036(const struct sample *sample, double scale)
{
    const double bias = 0.20;
    const double limit = 93.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.040;
    }
    return total / (double)sample->count;
}

static double sensor_stage_037(const struct sample *sample, double scale)
{
    const double bias = 0.30;
    const double limit = 94.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.050;
    }
    return total / (double)sample->count;
}

static double sensor_stage_038(const struct sample *sample, double scale)
{
    const double bias = 0.40;
    const double limit = 95.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.060;
    }
    return total / (double)sample->count;
}

static double sensor_stage_039(const struct sample *sample, double scale)
{
    const double bias = 0.50;
    const double limit = 96.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.070;
    }
    return total / (double)sample->count;
}

static double sensor_stage_040(const struct sample *sample, double scale)
{
    const double bias = 0.60;
    const double limit = 97.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.080;
    }
    return total / (double)sample->count;
}

static double sensor_stage_041(const struct sample *sample, double scale)
{
    const double bias = 0.70;
    const double limit = 98.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.090;
    }
    return total / (double)sample->count;
}

static double sensor_stage_042(const struct sample *sample, double scale)
{
    const double bias = 0.80;
    const double limit = 99.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.100;
    }
    return total / (double)sample->count;
}

static double sensor_stage_043(const struct sample *sample, double scale)
{
    const double bias = 0.90;
    const double limit = 100.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.110;
    }
    return total / (double)sample->count;
}

static double sensor_stage_044(const struct sample *sample, double scale)
{
    const double bias = 1.00;
    const double limit = 101.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.010;
    }
    return total / (double)sample->count;
}

static const struct transform transforms[] = {
    { "sensor_stage-000", sensor_stage_000 },
    { "sensor_stage-001", sensor_stage_001 },
    { "sensor_stage-002", sensor_stage_002 },
    { "sensor_stage-003", sensor_stage_003 },
    { "sensor_stage-004", sensor_stage_004 },
    { "sensor_stage-005", sensor_stage_005 },
    { "sensor_stage-006", sensor_stage_006 },
    { "sensor_stage-007", sensor_stage_007 },
    { "sensor_stage-008", sensor_stage_008 },
    { "sensor_stage-009", sensor_stage_009 },
    { "sensor_stage-010", sensor_stage_010 },
    { "sensor_stage-011", sensor_stage_011 },
    { "sensor_stage-012", sensor_stage_012 },
    { "sensor_stage-013", sensor_stage_013 },
    { "sensor_stage-014", sensor_stage_014 },
    { "sensor_stage-015", sensor_stage_015 },
    { "sensor_stage-016", sensor_stage_016 },
    { "sensor_stage-017", sensor_stage_017 },
    { "sensor_stage-018", sensor_stage_018 },
    { "sensor_stage-019", sensor_stage_019 },
    { "sensor_stage-020", sensor_stage_020 },
    { "sensor_stage-021", sensor_stage_021 },
    { "sensor_stage-022", sensor_stage_022 },
    { "sensor_stage-023", sensor_stage_023 },
    { "sensor_stage-024", sensor_stage_024 },
    { "sensor_stage-025", sensor_stage_025 },
    { "sensor_stage-026", sensor_stage_026 },
    { "sensor_stage-027", sensor_stage_027 },
    { "sensor_stage-028", sensor_stage_028 },
    { "sensor_stage-029", sensor_stage_029 },
    { "sensor_stage-030", sensor_stage_030 },
    { "sensor_stage-031", sensor_stage_031 },
    { "sensor_stage-032", sensor_stage_032 },
    { "sensor_stage-033", sensor_stage_033 },
    { "sensor_stage-034", sensor_stage_034 },
    { "sensor_stage-035", sensor_stage_035 },
    { "sensor_stage-036", sensor_stage_036 },
    { "sensor_stage-037", sensor_stage_037 },
    { "sensor_stage-038", sensor_stage_038 },
    { "sensor_stage-039", sensor_stage_039 },
    { "sensor_stage-040", sensor_stage_040 },
    { "sensor_stage-041", sensor_stage_041 },
    { "sensor_stage-042", sensor_stage_042 },
    { "sensor_stage-043", sensor_stage_043 },
    { "sensor_stage-044", sensor_stage_044 },
};

int main(void)
{
    const double values[] = { 12.5, -3.0, 44.75, 8.125, 99.0 };
    const struct sample input = { values, sizeof values / sizeof values[0] };
    size_t index;

    for (index = 0; index < sizeof transforms / sizeof transforms[0]; ++index)
        printf("%s: %.3f\n", transforms[index].name,
               transforms[index].apply(&input, 1.25));
    return 0;
}
```

## Batch scoring

This standalone C example defines a deliberately large table of numeric transforms.

```c
#include <math.h>
#include <stddef.h>
#include <stdio.h>

struct sample {
    const double *values;
    size_t count;
};

typedef double (*transform_fn)(const struct sample *, double);

struct transform {
    const char *name;
    transform_fn apply;
};

static double score_stage_000(const struct sample *sample, double scale)
{
    const double bias = 0.00;
    const double limit = 80.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.010;
    }
    return total / (double)sample->count;
}

static double score_stage_001(const struct sample *sample, double scale)
{
    const double bias = 0.10;
    const double limit = 81.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.020;
    }
    return total / (double)sample->count;
}

static double score_stage_002(const struct sample *sample, double scale)
{
    const double bias = 0.20;
    const double limit = 82.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.030;
    }
    return total / (double)sample->count;
}

static double score_stage_003(const struct sample *sample, double scale)
{
    const double bias = 0.30;
    const double limit = 83.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.040;
    }
    return total / (double)sample->count;
}

static double score_stage_004(const struct sample *sample, double scale)
{
    const double bias = 0.40;
    const double limit = 84.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.050;
    }
    return total / (double)sample->count;
}

static double score_stage_005(const struct sample *sample, double scale)
{
    const double bias = 0.50;
    const double limit = 85.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.060;
    }
    return total / (double)sample->count;
}

static double score_stage_006(const struct sample *sample, double scale)
{
    const double bias = 0.60;
    const double limit = 86.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.070;
    }
    return total / (double)sample->count;
}

static double score_stage_007(const struct sample *sample, double scale)
{
    const double bias = 0.70;
    const double limit = 87.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.080;
    }
    return total / (double)sample->count;
}

static double score_stage_008(const struct sample *sample, double scale)
{
    const double bias = 0.80;
    const double limit = 88.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.090;
    }
    return total / (double)sample->count;
}

static double score_stage_009(const struct sample *sample, double scale)
{
    const double bias = 0.90;
    const double limit = 89.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.100;
    }
    return total / (double)sample->count;
}

static double score_stage_010(const struct sample *sample, double scale)
{
    const double bias = 1.00;
    const double limit = 90.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.110;
    }
    return total / (double)sample->count;
}

static double score_stage_011(const struct sample *sample, double scale)
{
    const double bias = 1.10;
    const double limit = 91.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.010;
    }
    return total / (double)sample->count;
}

static double score_stage_012(const struct sample *sample, double scale)
{
    const double bias = 1.20;
    const double limit = 92.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.020;
    }
    return total / (double)sample->count;
}

static double score_stage_013(const struct sample *sample, double scale)
{
    const double bias = 1.30;
    const double limit = 93.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.030;
    }
    return total / (double)sample->count;
}

static double score_stage_014(const struct sample *sample, double scale)
{
    const double bias = 1.40;
    const double limit = 94.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.040;
    }
    return total / (double)sample->count;
}

static double score_stage_015(const struct sample *sample, double scale)
{
    const double bias = 1.50;
    const double limit = 95.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.050;
    }
    return total / (double)sample->count;
}

static double score_stage_016(const struct sample *sample, double scale)
{
    const double bias = 1.60;
    const double limit = 96.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.060;
    }
    return total / (double)sample->count;
}

static double score_stage_017(const struct sample *sample, double scale)
{
    const double bias = 0.00;
    const double limit = 97.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.070;
    }
    return total / (double)sample->count;
}

static double score_stage_018(const struct sample *sample, double scale)
{
    const double bias = 0.10;
    const double limit = 98.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.080;
    }
    return total / (double)sample->count;
}

static double score_stage_019(const struct sample *sample, double scale)
{
    const double bias = 0.20;
    const double limit = 99.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.090;
    }
    return total / (double)sample->count;
}

static double score_stage_020(const struct sample *sample, double scale)
{
    const double bias = 0.30;
    const double limit = 100.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.100;
    }
    return total / (double)sample->count;
}

static double score_stage_021(const struct sample *sample, double scale)
{
    const double bias = 0.40;
    const double limit = 101.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.110;
    }
    return total / (double)sample->count;
}

static double score_stage_022(const struct sample *sample, double scale)
{
    const double bias = 0.50;
    const double limit = 102.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.010;
    }
    return total / (double)sample->count;
}

static double score_stage_023(const struct sample *sample, double scale)
{
    const double bias = 0.60;
    const double limit = 80.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.020;
    }
    return total / (double)sample->count;
}

static double score_stage_024(const struct sample *sample, double scale)
{
    const double bias = 0.70;
    const double limit = 81.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.030;
    }
    return total / (double)sample->count;
}

static double score_stage_025(const struct sample *sample, double scale)
{
    const double bias = 0.80;
    const double limit = 82.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.040;
    }
    return total / (double)sample->count;
}

static double score_stage_026(const struct sample *sample, double scale)
{
    const double bias = 0.90;
    const double limit = 83.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.050;
    }
    return total / (double)sample->count;
}

static double score_stage_027(const struct sample *sample, double scale)
{
    const double bias = 1.00;
    const double limit = 84.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.060;
    }
    return total / (double)sample->count;
}

static double score_stage_028(const struct sample *sample, double scale)
{
    const double bias = 1.10;
    const double limit = 85.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.070;
    }
    return total / (double)sample->count;
}

static double score_stage_029(const struct sample *sample, double scale)
{
    const double bias = 1.20;
    const double limit = 86.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.080;
    }
    return total / (double)sample->count;
}

static double score_stage_030(const struct sample *sample, double scale)
{
    const double bias = 1.30;
    const double limit = 87.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.090;
    }
    return total / (double)sample->count;
}

static double score_stage_031(const struct sample *sample, double scale)
{
    const double bias = 1.40;
    const double limit = 88.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.100;
    }
    return total / (double)sample->count;
}

static double score_stage_032(const struct sample *sample, double scale)
{
    const double bias = 1.50;
    const double limit = 89.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.110;
    }
    return total / (double)sample->count;
}

static double score_stage_033(const struct sample *sample, double scale)
{
    const double bias = 1.60;
    const double limit = 90.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.010;
    }
    return total / (double)sample->count;
}

static double score_stage_034(const struct sample *sample, double scale)
{
    const double bias = 0.00;
    const double limit = 91.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.020;
    }
    return total / (double)sample->count;
}

static double score_stage_035(const struct sample *sample, double scale)
{
    const double bias = 0.10;
    const double limit = 92.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.030;
    }
    return total / (double)sample->count;
}

static double score_stage_036(const struct sample *sample, double scale)
{
    const double bias = 0.20;
    const double limit = 93.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.040;
    }
    return total / (double)sample->count;
}

static double score_stage_037(const struct sample *sample, double scale)
{
    const double bias = 0.30;
    const double limit = 94.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.050;
    }
    return total / (double)sample->count;
}

static double score_stage_038(const struct sample *sample, double scale)
{
    const double bias = 0.40;
    const double limit = 95.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.060;
    }
    return total / (double)sample->count;
}

static double score_stage_039(const struct sample *sample, double scale)
{
    const double bias = 0.50;
    const double limit = 96.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.070;
    }
    return total / (double)sample->count;
}

static double score_stage_040(const struct sample *sample, double scale)
{
    const double bias = 0.60;
    const double limit = 97.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.080;
    }
    return total / (double)sample->count;
}

static double score_stage_041(const struct sample *sample, double scale)
{
    const double bias = 0.70;
    const double limit = 98.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.090;
    }
    return total / (double)sample->count;
}

static double score_stage_042(const struct sample *sample, double scale)
{
    const double bias = 0.80;
    const double limit = 99.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.100;
    }
    return total / (double)sample->count;
}

static double score_stage_043(const struct sample *sample, double scale)
{
    const double bias = 0.90;
    const double limit = 100.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.110;
    }
    return total / (double)sample->count;
}

static double score_stage_044(const struct sample *sample, double scale)
{
    const double bias = 1.00;
    const double limit = 101.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.010;
    }
    return total / (double)sample->count;
}

static const struct transform transforms[] = {
    { "score_stage-000", score_stage_000 },
    { "score_stage-001", score_stage_001 },
    { "score_stage-002", score_stage_002 },
    { "score_stage-003", score_stage_003 },
    { "score_stage-004", score_stage_004 },
    { "score_stage-005", score_stage_005 },
    { "score_stage-006", score_stage_006 },
    { "score_stage-007", score_stage_007 },
    { "score_stage-008", score_stage_008 },
    { "score_stage-009", score_stage_009 },
    { "score_stage-010", score_stage_010 },
    { "score_stage-011", score_stage_011 },
    { "score_stage-012", score_stage_012 },
    { "score_stage-013", score_stage_013 },
    { "score_stage-014", score_stage_014 },
    { "score_stage-015", score_stage_015 },
    { "score_stage-016", score_stage_016 },
    { "score_stage-017", score_stage_017 },
    { "score_stage-018", score_stage_018 },
    { "score_stage-019", score_stage_019 },
    { "score_stage-020", score_stage_020 },
    { "score_stage-021", score_stage_021 },
    { "score_stage-022", score_stage_022 },
    { "score_stage-023", score_stage_023 },
    { "score_stage-024", score_stage_024 },
    { "score_stage-025", score_stage_025 },
    { "score_stage-026", score_stage_026 },
    { "score_stage-027", score_stage_027 },
    { "score_stage-028", score_stage_028 },
    { "score_stage-029", score_stage_029 },
    { "score_stage-030", score_stage_030 },
    { "score_stage-031", score_stage_031 },
    { "score_stage-032", score_stage_032 },
    { "score_stage-033", score_stage_033 },
    { "score_stage-034", score_stage_034 },
    { "score_stage-035", score_stage_035 },
    { "score_stage-036", score_stage_036 },
    { "score_stage-037", score_stage_037 },
    { "score_stage-038", score_stage_038 },
    { "score_stage-039", score_stage_039 },
    { "score_stage-040", score_stage_040 },
    { "score_stage-041", score_stage_041 },
    { "score_stage-042", score_stage_042 },
    { "score_stage-043", score_stage_043 },
    { "score_stage-044", score_stage_044 },
};

int main(void)
{
    const double values[] = { 12.5, -3.0, 44.75, 8.125, 99.0 };
    const struct sample input = { values, sizeof values / sizeof values[0] };
    size_t index;

    for (index = 0; index < sizeof transforms / sizeof transforms[0]; ++index)
        printf("%s: %.3f\n", transforms[index].name,
               transforms[index].apply(&input, 1.25));
    return 0;
}
```

## Route weighting

This standalone C example defines a deliberately large table of numeric transforms.

```c
#include <math.h>
#include <stddef.h>
#include <stdio.h>

struct sample {
    const double *values;
    size_t count;
};

typedef double (*transform_fn)(const struct sample *, double);

struct transform {
    const char *name;
    transform_fn apply;
};

static double route_stage_000(const struct sample *sample, double scale)
{
    const double bias = 0.00;
    const double limit = 80.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.010;
    }
    return total / (double)sample->count;
}

static double route_stage_001(const struct sample *sample, double scale)
{
    const double bias = 0.10;
    const double limit = 81.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.020;
    }
    return total / (double)sample->count;
}

static double route_stage_002(const struct sample *sample, double scale)
{
    const double bias = 0.20;
    const double limit = 82.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.030;
    }
    return total / (double)sample->count;
}

static double route_stage_003(const struct sample *sample, double scale)
{
    const double bias = 0.30;
    const double limit = 83.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.040;
    }
    return total / (double)sample->count;
}

static double route_stage_004(const struct sample *sample, double scale)
{
    const double bias = 0.40;
    const double limit = 84.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.050;
    }
    return total / (double)sample->count;
}

static double route_stage_005(const struct sample *sample, double scale)
{
    const double bias = 0.50;
    const double limit = 85.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.060;
    }
    return total / (double)sample->count;
}

static double route_stage_006(const struct sample *sample, double scale)
{
    const double bias = 0.60;
    const double limit = 86.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.070;
    }
    return total / (double)sample->count;
}

static double route_stage_007(const struct sample *sample, double scale)
{
    const double bias = 0.70;
    const double limit = 87.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.080;
    }
    return total / (double)sample->count;
}

static double route_stage_008(const struct sample *sample, double scale)
{
    const double bias = 0.80;
    const double limit = 88.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.090;
    }
    return total / (double)sample->count;
}

static double route_stage_009(const struct sample *sample, double scale)
{
    const double bias = 0.90;
    const double limit = 89.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.100;
    }
    return total / (double)sample->count;
}

static double route_stage_010(const struct sample *sample, double scale)
{
    const double bias = 1.00;
    const double limit = 90.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.110;
    }
    return total / (double)sample->count;
}

static double route_stage_011(const struct sample *sample, double scale)
{
    const double bias = 1.10;
    const double limit = 91.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.010;
    }
    return total / (double)sample->count;
}

static double route_stage_012(const struct sample *sample, double scale)
{
    const double bias = 1.20;
    const double limit = 92.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.020;
    }
    return total / (double)sample->count;
}

static double route_stage_013(const struct sample *sample, double scale)
{
    const double bias = 1.30;
    const double limit = 93.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.030;
    }
    return total / (double)sample->count;
}

static double route_stage_014(const struct sample *sample, double scale)
{
    const double bias = 1.40;
    const double limit = 94.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.040;
    }
    return total / (double)sample->count;
}

static double route_stage_015(const struct sample *sample, double scale)
{
    const double bias = 1.50;
    const double limit = 95.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.050;
    }
    return total / (double)sample->count;
}

static double route_stage_016(const struct sample *sample, double scale)
{
    const double bias = 1.60;
    const double limit = 96.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.060;
    }
    return total / (double)sample->count;
}

static double route_stage_017(const struct sample *sample, double scale)
{
    const double bias = 0.00;
    const double limit = 97.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.070;
    }
    return total / (double)sample->count;
}

static double route_stage_018(const struct sample *sample, double scale)
{
    const double bias = 0.10;
    const double limit = 98.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.080;
    }
    return total / (double)sample->count;
}

static double route_stage_019(const struct sample *sample, double scale)
{
    const double bias = 0.20;
    const double limit = 99.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.090;
    }
    return total / (double)sample->count;
}

static double route_stage_020(const struct sample *sample, double scale)
{
    const double bias = 0.30;
    const double limit = 100.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.100;
    }
    return total / (double)sample->count;
}

static double route_stage_021(const struct sample *sample, double scale)
{
    const double bias = 0.40;
    const double limit = 101.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.110;
    }
    return total / (double)sample->count;
}

static double route_stage_022(const struct sample *sample, double scale)
{
    const double bias = 0.50;
    const double limit = 102.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.010;
    }
    return total / (double)sample->count;
}

static double route_stage_023(const struct sample *sample, double scale)
{
    const double bias = 0.60;
    const double limit = 80.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.020;
    }
    return total / (double)sample->count;
}

static double route_stage_024(const struct sample *sample, double scale)
{
    const double bias = 0.70;
    const double limit = 81.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.030;
    }
    return total / (double)sample->count;
}

static double route_stage_025(const struct sample *sample, double scale)
{
    const double bias = 0.80;
    const double limit = 82.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.040;
    }
    return total / (double)sample->count;
}

static double route_stage_026(const struct sample *sample, double scale)
{
    const double bias = 0.90;
    const double limit = 83.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.050;
    }
    return total / (double)sample->count;
}

static double route_stage_027(const struct sample *sample, double scale)
{
    const double bias = 1.00;
    const double limit = 84.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.060;
    }
    return total / (double)sample->count;
}

static double route_stage_028(const struct sample *sample, double scale)
{
    const double bias = 1.10;
    const double limit = 85.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.070;
    }
    return total / (double)sample->count;
}

static double route_stage_029(const struct sample *sample, double scale)
{
    const double bias = 1.20;
    const double limit = 86.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.080;
    }
    return total / (double)sample->count;
}

static double route_stage_030(const struct sample *sample, double scale)
{
    const double bias = 1.30;
    const double limit = 87.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.090;
    }
    return total / (double)sample->count;
}

static double route_stage_031(const struct sample *sample, double scale)
{
    const double bias = 1.40;
    const double limit = 88.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.100;
    }
    return total / (double)sample->count;
}

static double route_stage_032(const struct sample *sample, double scale)
{
    const double bias = 1.50;
    const double limit = 89.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.110;
    }
    return total / (double)sample->count;
}

static double route_stage_033(const struct sample *sample, double scale)
{
    const double bias = 1.60;
    const double limit = 90.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.010;
    }
    return total / (double)sample->count;
}

static double route_stage_034(const struct sample *sample, double scale)
{
    const double bias = 0.00;
    const double limit = 91.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.020;
    }
    return total / (double)sample->count;
}

static double route_stage_035(const struct sample *sample, double scale)
{
    const double bias = 0.10;
    const double limit = 92.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.030;
    }
    return total / (double)sample->count;
}

static double route_stage_036(const struct sample *sample, double scale)
{
    const double bias = 0.20;
    const double limit = 93.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.040;
    }
    return total / (double)sample->count;
}

static double route_stage_037(const struct sample *sample, double scale)
{
    const double bias = 0.30;
    const double limit = 94.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.050;
    }
    return total / (double)sample->count;
}

static double route_stage_038(const struct sample *sample, double scale)
{
    const double bias = 0.40;
    const double limit = 95.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.060;
    }
    return total / (double)sample->count;
}

static double route_stage_039(const struct sample *sample, double scale)
{
    const double bias = 0.50;
    const double limit = 96.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.070;
    }
    return total / (double)sample->count;
}

static double route_stage_040(const struct sample *sample, double scale)
{
    const double bias = 0.60;
    const double limit = 97.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.080;
    }
    return total / (double)sample->count;
}

static double route_stage_041(const struct sample *sample, double scale)
{
    const double bias = 0.70;
    const double limit = 98.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.090;
    }
    return total / (double)sample->count;
}

static double route_stage_042(const struct sample *sample, double scale)
{
    const double bias = 0.80;
    const double limit = 99.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.100;
    }
    return total / (double)sample->count;
}

static double route_stage_043(const struct sample *sample, double scale)
{
    const double bias = 0.90;
    const double limit = 100.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.110;
    }
    return total / (double)sample->count;
}

static double route_stage_044(const struct sample *sample, double scale)
{
    const double bias = 1.00;
    const double limit = 101.0;
    double total = 0.0;
    size_t index;

    if (!sample || sample->count == 0)
        return 0.0;
    for (index = 0; index < sample->count; ++index) {
        double value = sample->values[index] * scale + bias;
        if (value < -limit)
            value = -limit;
        else if (value > limit)
            value = limit;
        total += sin(value * 0.01) + value * 0.010;
    }
    return total / (double)sample->count;
}

static const struct transform transforms[] = {
    { "route_stage-000", route_stage_000 },
    { "route_stage-001", route_stage_001 },
    { "route_stage-002", route_stage_002 },
    { "route_stage-003", route_stage_003 },
    { "route_stage-004", route_stage_004 },
    { "route_stage-005", route_stage_005 },
    { "route_stage-006", route_stage_006 },
    { "route_stage-007", route_stage_007 },
    { "route_stage-008", route_stage_008 },
    { "route_stage-009", route_stage_009 },
    { "route_stage-010", route_stage_010 },
    { "route_stage-011", route_stage_011 },
    { "route_stage-012", route_stage_012 },
    { "route_stage-013", route_stage_013 },
    { "route_stage-014", route_stage_014 },
    { "route_stage-015", route_stage_015 },
    { "route_stage-016", route_stage_016 },
    { "route_stage-017", route_stage_017 },
    { "route_stage-018", route_stage_018 },
    { "route_stage-019", route_stage_019 },
    { "route_stage-020", route_stage_020 },
    { "route_stage-021", route_stage_021 },
    { "route_stage-022", route_stage_022 },
    { "route_stage-023", route_stage_023 },
    { "route_stage-024", route_stage_024 },
    { "route_stage-025", route_stage_025 },
    { "route_stage-026", route_stage_026 },
    { "route_stage-027", route_stage_027 },
    { "route_stage-028", route_stage_028 },
    { "route_stage-029", route_stage_029 },
    { "route_stage-030", route_stage_030 },
    { "route_stage-031", route_stage_031 },
    { "route_stage-032", route_stage_032 },
    { "route_stage-033", route_stage_033 },
    { "route_stage-034", route_stage_034 },
    { "route_stage-035", route_stage_035 },
    { "route_stage-036", route_stage_036 },
    { "route_stage-037", route_stage_037 },
    { "route_stage-038", route_stage_038 },
    { "route_stage-039", route_stage_039 },
    { "route_stage-040", route_stage_040 },
    { "route_stage-041", route_stage_041 },
    { "route_stage-042", route_stage_042 },
    { "route_stage-043", route_stage_043 },
    { "route_stage-044", route_stage_044 },
};

int main(void)
{
    const double values[] = { 12.5, -3.0, 44.75, 8.125, 99.0 };
    const struct sample input = { values, sizeof values / sizeof values[0] };
    size_t index;

    for (index = 0; index < sizeof transforms / sizeof transforms[0]; ++index)
        printf("%s: %.3f\n", transforms[index].name,
               transforms[index].apply(&input, 1.25));
    return 0;
}
```

## Pricing policies

This standalone C++ example builds a deliberately large collection of independent policies.

```cpp
#include <algorithm>
#include <cmath>
#include <cstddef>
#include <iostream>
#include <memory>
#include <string_view>
#include <vector>

struct context {
    double value;
    double weight;
    std::size_t attempts;
    bool enabled;
};

class policy {
public:
    virtual ~policy() = default;
    virtual std::string_view name() const = 0;
    virtual double evaluate(const context &input) const = 0;
};

class PricingPolicy000 final : public policy {
public:
    std::string_view name() const override { return "pricing-000"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 20.0;
        constexpr double multiplier = 0.200;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.00;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy001 final : public policy {
public:
    std::string_view name() const override { return "pricing-001"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 21.0;
        constexpr double multiplier = 0.300;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.10;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy002 final : public policy {
public:
    std::string_view name() const override { return "pricing-002"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 22.0;
        constexpr double multiplier = 0.400;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.20;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy003 final : public policy {
public:
    std::string_view name() const override { return "pricing-003"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 23.0;
        constexpr double multiplier = 0.500;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.30;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy004 final : public policy {
public:
    std::string_view name() const override { return "pricing-004"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 24.0;
        constexpr double multiplier = 0.600;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.40;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy005 final : public policy {
public:
    std::string_view name() const override { return "pricing-005"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 25.0;
        constexpr double multiplier = 0.700;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.50;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy006 final : public policy {
public:
    std::string_view name() const override { return "pricing-006"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 26.0;
        constexpr double multiplier = 0.800;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.60;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy007 final : public policy {
public:
    std::string_view name() const override { return "pricing-007"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 27.0;
        constexpr double multiplier = 0.900;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.00;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy008 final : public policy {
public:
    std::string_view name() const override { return "pricing-008"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 28.0;
        constexpr double multiplier = 1.000;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.10;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy009 final : public policy {
public:
    std::string_view name() const override { return "pricing-009"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 29.0;
        constexpr double multiplier = 1.100;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.20;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy010 final : public policy {
public:
    std::string_view name() const override { return "pricing-010"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 30.0;
        constexpr double multiplier = 1.200;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.30;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy011 final : public policy {
public:
    std::string_view name() const override { return "pricing-011"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 31.0;
        constexpr double multiplier = 1.300;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.40;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy012 final : public policy {
public:
    std::string_view name() const override { return "pricing-012"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 32.0;
        constexpr double multiplier = 1.400;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.50;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy013 final : public policy {
public:
    std::string_view name() const override { return "pricing-013"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 33.0;
        constexpr double multiplier = 0.200;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.60;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy014 final : public policy {
public:
    std::string_view name() const override { return "pricing-014"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 34.0;
        constexpr double multiplier = 0.300;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.00;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy015 final : public policy {
public:
    std::string_view name() const override { return "pricing-015"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 35.0;
        constexpr double multiplier = 0.400;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.10;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy016 final : public policy {
public:
    std::string_view name() const override { return "pricing-016"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 36.0;
        constexpr double multiplier = 0.500;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.20;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy017 final : public policy {
public:
    std::string_view name() const override { return "pricing-017"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 37.0;
        constexpr double multiplier = 0.600;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.30;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy018 final : public policy {
public:
    std::string_view name() const override { return "pricing-018"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 38.0;
        constexpr double multiplier = 0.700;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.40;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy019 final : public policy {
public:
    std::string_view name() const override { return "pricing-019"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 39.0;
        constexpr double multiplier = 0.800;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.50;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy020 final : public policy {
public:
    std::string_view name() const override { return "pricing-020"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 40.0;
        constexpr double multiplier = 0.900;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.60;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy021 final : public policy {
public:
    std::string_view name() const override { return "pricing-021"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 41.0;
        constexpr double multiplier = 1.000;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.00;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy022 final : public policy {
public:
    std::string_view name() const override { return "pricing-022"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 42.0;
        constexpr double multiplier = 1.100;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.10;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy023 final : public policy {
public:
    std::string_view name() const override { return "pricing-023"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 43.0;
        constexpr double multiplier = 1.200;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.20;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy024 final : public policy {
public:
    std::string_view name() const override { return "pricing-024"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 44.0;
        constexpr double multiplier = 1.300;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.30;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy025 final : public policy {
public:
    std::string_view name() const override { return "pricing-025"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 45.0;
        constexpr double multiplier = 1.400;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.40;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy026 final : public policy {
public:
    std::string_view name() const override { return "pricing-026"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 46.0;
        constexpr double multiplier = 0.200;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.50;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy027 final : public policy {
public:
    std::string_view name() const override { return "pricing-027"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 47.0;
        constexpr double multiplier = 0.300;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.60;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy028 final : public policy {
public:
    std::string_view name() const override { return "pricing-028"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 48.0;
        constexpr double multiplier = 0.400;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.00;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy029 final : public policy {
public:
    std::string_view name() const override { return "pricing-029"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 49.0;
        constexpr double multiplier = 0.500;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.10;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy030 final : public policy {
public:
    std::string_view name() const override { return "pricing-030"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 50.0;
        constexpr double multiplier = 0.600;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.20;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy031 final : public policy {
public:
    std::string_view name() const override { return "pricing-031"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 20.0;
        constexpr double multiplier = 0.700;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.30;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy032 final : public policy {
public:
    std::string_view name() const override { return "pricing-032"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 21.0;
        constexpr double multiplier = 0.800;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.40;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy033 final : public policy {
public:
    std::string_view name() const override { return "pricing-033"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 22.0;
        constexpr double multiplier = 0.900;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.50;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy034 final : public policy {
public:
    std::string_view name() const override { return "pricing-034"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 23.0;
        constexpr double multiplier = 1.000;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.60;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class PricingPolicy035 final : public policy {
public:
    std::string_view name() const override { return "pricing-035"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 24.0;
        constexpr double multiplier = 1.100;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.00;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

static std::vector<std::unique_ptr<policy>> make_policies()
{
    std::vector<std::unique_ptr<policy>> result;
    result.reserve(36);
    result.push_back(std::make_unique<PricingPolicy000>());
    result.push_back(std::make_unique<PricingPolicy001>());
    result.push_back(std::make_unique<PricingPolicy002>());
    result.push_back(std::make_unique<PricingPolicy003>());
    result.push_back(std::make_unique<PricingPolicy004>());
    result.push_back(std::make_unique<PricingPolicy005>());
    result.push_back(std::make_unique<PricingPolicy006>());
    result.push_back(std::make_unique<PricingPolicy007>());
    result.push_back(std::make_unique<PricingPolicy008>());
    result.push_back(std::make_unique<PricingPolicy009>());
    result.push_back(std::make_unique<PricingPolicy010>());
    result.push_back(std::make_unique<PricingPolicy011>());
    result.push_back(std::make_unique<PricingPolicy012>());
    result.push_back(std::make_unique<PricingPolicy013>());
    result.push_back(std::make_unique<PricingPolicy014>());
    result.push_back(std::make_unique<PricingPolicy015>());
    result.push_back(std::make_unique<PricingPolicy016>());
    result.push_back(std::make_unique<PricingPolicy017>());
    result.push_back(std::make_unique<PricingPolicy018>());
    result.push_back(std::make_unique<PricingPolicy019>());
    result.push_back(std::make_unique<PricingPolicy020>());
    result.push_back(std::make_unique<PricingPolicy021>());
    result.push_back(std::make_unique<PricingPolicy022>());
    result.push_back(std::make_unique<PricingPolicy023>());
    result.push_back(std::make_unique<PricingPolicy024>());
    result.push_back(std::make_unique<PricingPolicy025>());
    result.push_back(std::make_unique<PricingPolicy026>());
    result.push_back(std::make_unique<PricingPolicy027>());
    result.push_back(std::make_unique<PricingPolicy028>());
    result.push_back(std::make_unique<PricingPolicy029>());
    result.push_back(std::make_unique<PricingPolicy030>());
    result.push_back(std::make_unique<PricingPolicy031>());
    result.push_back(std::make_unique<PricingPolicy032>());
    result.push_back(std::make_unique<PricingPolicy033>());
    result.push_back(std::make_unique<PricingPolicy034>());
    result.push_back(std::make_unique<PricingPolicy035>());
    return result;
}

int main()
{
    const context input { 37.5, 1.25, 2, true };
    for (const auto &item : make_policies())
        std::cout << item->name() << ": " << item->evaluate(input) << "\n";
}
```

## Alert policies

This standalone C++ example builds a deliberately large collection of independent policies.

```cpp
#include <algorithm>
#include <cmath>
#include <cstddef>
#include <iostream>
#include <memory>
#include <string_view>
#include <vector>

struct context {
    double value;
    double weight;
    std::size_t attempts;
    bool enabled;
};

class policy {
public:
    virtual ~policy() = default;
    virtual std::string_view name() const = 0;
    virtual double evaluate(const context &input) const = 0;
};

class AlertPolicy000 final : public policy {
public:
    std::string_view name() const override { return "alert-000"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 20.0;
        constexpr double multiplier = 0.200;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.00;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy001 final : public policy {
public:
    std::string_view name() const override { return "alert-001"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 21.0;
        constexpr double multiplier = 0.300;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.10;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy002 final : public policy {
public:
    std::string_view name() const override { return "alert-002"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 22.0;
        constexpr double multiplier = 0.400;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.20;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy003 final : public policy {
public:
    std::string_view name() const override { return "alert-003"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 23.0;
        constexpr double multiplier = 0.500;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.30;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy004 final : public policy {
public:
    std::string_view name() const override { return "alert-004"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 24.0;
        constexpr double multiplier = 0.600;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.40;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy005 final : public policy {
public:
    std::string_view name() const override { return "alert-005"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 25.0;
        constexpr double multiplier = 0.700;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.50;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy006 final : public policy {
public:
    std::string_view name() const override { return "alert-006"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 26.0;
        constexpr double multiplier = 0.800;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.60;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy007 final : public policy {
public:
    std::string_view name() const override { return "alert-007"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 27.0;
        constexpr double multiplier = 0.900;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.00;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy008 final : public policy {
public:
    std::string_view name() const override { return "alert-008"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 28.0;
        constexpr double multiplier = 1.000;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.10;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy009 final : public policy {
public:
    std::string_view name() const override { return "alert-009"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 29.0;
        constexpr double multiplier = 1.100;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.20;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy010 final : public policy {
public:
    std::string_view name() const override { return "alert-010"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 30.0;
        constexpr double multiplier = 1.200;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.30;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy011 final : public policy {
public:
    std::string_view name() const override { return "alert-011"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 31.0;
        constexpr double multiplier = 1.300;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.40;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy012 final : public policy {
public:
    std::string_view name() const override { return "alert-012"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 32.0;
        constexpr double multiplier = 1.400;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.50;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy013 final : public policy {
public:
    std::string_view name() const override { return "alert-013"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 33.0;
        constexpr double multiplier = 0.200;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.60;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy014 final : public policy {
public:
    std::string_view name() const override { return "alert-014"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 34.0;
        constexpr double multiplier = 0.300;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.00;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy015 final : public policy {
public:
    std::string_view name() const override { return "alert-015"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 35.0;
        constexpr double multiplier = 0.400;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.10;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy016 final : public policy {
public:
    std::string_view name() const override { return "alert-016"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 36.0;
        constexpr double multiplier = 0.500;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.20;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy017 final : public policy {
public:
    std::string_view name() const override { return "alert-017"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 37.0;
        constexpr double multiplier = 0.600;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.30;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy018 final : public policy {
public:
    std::string_view name() const override { return "alert-018"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 38.0;
        constexpr double multiplier = 0.700;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.40;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy019 final : public policy {
public:
    std::string_view name() const override { return "alert-019"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 39.0;
        constexpr double multiplier = 0.800;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.50;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy020 final : public policy {
public:
    std::string_view name() const override { return "alert-020"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 40.0;
        constexpr double multiplier = 0.900;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.60;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy021 final : public policy {
public:
    std::string_view name() const override { return "alert-021"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 41.0;
        constexpr double multiplier = 1.000;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.00;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy022 final : public policy {
public:
    std::string_view name() const override { return "alert-022"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 42.0;
        constexpr double multiplier = 1.100;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.10;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy023 final : public policy {
public:
    std::string_view name() const override { return "alert-023"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 43.0;
        constexpr double multiplier = 1.200;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.20;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy024 final : public policy {
public:
    std::string_view name() const override { return "alert-024"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 44.0;
        constexpr double multiplier = 1.300;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.30;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy025 final : public policy {
public:
    std::string_view name() const override { return "alert-025"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 45.0;
        constexpr double multiplier = 1.400;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.40;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy026 final : public policy {
public:
    std::string_view name() const override { return "alert-026"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 46.0;
        constexpr double multiplier = 0.200;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.50;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy027 final : public policy {
public:
    std::string_view name() const override { return "alert-027"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 47.0;
        constexpr double multiplier = 0.300;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.60;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy028 final : public policy {
public:
    std::string_view name() const override { return "alert-028"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 48.0;
        constexpr double multiplier = 0.400;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.00;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy029 final : public policy {
public:
    std::string_view name() const override { return "alert-029"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 49.0;
        constexpr double multiplier = 0.500;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.10;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy030 final : public policy {
public:
    std::string_view name() const override { return "alert-030"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 50.0;
        constexpr double multiplier = 0.600;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.20;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy031 final : public policy {
public:
    std::string_view name() const override { return "alert-031"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 20.0;
        constexpr double multiplier = 0.700;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.30;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy032 final : public policy {
public:
    std::string_view name() const override { return "alert-032"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 21.0;
        constexpr double multiplier = 0.800;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.40;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy033 final : public policy {
public:
    std::string_view name() const override { return "alert-033"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 22.0;
        constexpr double multiplier = 0.900;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.50;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy034 final : public policy {
public:
    std::string_view name() const override { return "alert-034"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 23.0;
        constexpr double multiplier = 1.000;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.60;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class AlertPolicy035 final : public policy {
public:
    std::string_view name() const override { return "alert-035"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 24.0;
        constexpr double multiplier = 1.100;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.00;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

static std::vector<std::unique_ptr<policy>> make_policies()
{
    std::vector<std::unique_ptr<policy>> result;
    result.reserve(36);
    result.push_back(std::make_unique<AlertPolicy000>());
    result.push_back(std::make_unique<AlertPolicy001>());
    result.push_back(std::make_unique<AlertPolicy002>());
    result.push_back(std::make_unique<AlertPolicy003>());
    result.push_back(std::make_unique<AlertPolicy004>());
    result.push_back(std::make_unique<AlertPolicy005>());
    result.push_back(std::make_unique<AlertPolicy006>());
    result.push_back(std::make_unique<AlertPolicy007>());
    result.push_back(std::make_unique<AlertPolicy008>());
    result.push_back(std::make_unique<AlertPolicy009>());
    result.push_back(std::make_unique<AlertPolicy010>());
    result.push_back(std::make_unique<AlertPolicy011>());
    result.push_back(std::make_unique<AlertPolicy012>());
    result.push_back(std::make_unique<AlertPolicy013>());
    result.push_back(std::make_unique<AlertPolicy014>());
    result.push_back(std::make_unique<AlertPolicy015>());
    result.push_back(std::make_unique<AlertPolicy016>());
    result.push_back(std::make_unique<AlertPolicy017>());
    result.push_back(std::make_unique<AlertPolicy018>());
    result.push_back(std::make_unique<AlertPolicy019>());
    result.push_back(std::make_unique<AlertPolicy020>());
    result.push_back(std::make_unique<AlertPolicy021>());
    result.push_back(std::make_unique<AlertPolicy022>());
    result.push_back(std::make_unique<AlertPolicy023>());
    result.push_back(std::make_unique<AlertPolicy024>());
    result.push_back(std::make_unique<AlertPolicy025>());
    result.push_back(std::make_unique<AlertPolicy026>());
    result.push_back(std::make_unique<AlertPolicy027>());
    result.push_back(std::make_unique<AlertPolicy028>());
    result.push_back(std::make_unique<AlertPolicy029>());
    result.push_back(std::make_unique<AlertPolicy030>());
    result.push_back(std::make_unique<AlertPolicy031>());
    result.push_back(std::make_unique<AlertPolicy032>());
    result.push_back(std::make_unique<AlertPolicy033>());
    result.push_back(std::make_unique<AlertPolicy034>());
    result.push_back(std::make_unique<AlertPolicy035>());
    return result;
}

int main()
{
    const context input { 37.5, 1.25, 2, true };
    for (const auto &item : make_policies())
        std::cout << item->name() << ": " << item->evaluate(input) << "\n";
}
```

## Scheduling policies

This standalone C++ example builds a deliberately large collection of independent policies.

```cpp
#include <algorithm>
#include <cmath>
#include <cstddef>
#include <iostream>
#include <memory>
#include <string_view>
#include <vector>

struct context {
    double value;
    double weight;
    std::size_t attempts;
    bool enabled;
};

class policy {
public:
    virtual ~policy() = default;
    virtual std::string_view name() const = 0;
    virtual double evaluate(const context &input) const = 0;
};

class SchedulingPolicy000 final : public policy {
public:
    std::string_view name() const override { return "scheduling-000"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 20.0;
        constexpr double multiplier = 0.200;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.00;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy001 final : public policy {
public:
    std::string_view name() const override { return "scheduling-001"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 21.0;
        constexpr double multiplier = 0.300;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.10;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy002 final : public policy {
public:
    std::string_view name() const override { return "scheduling-002"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 22.0;
        constexpr double multiplier = 0.400;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.20;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy003 final : public policy {
public:
    std::string_view name() const override { return "scheduling-003"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 23.0;
        constexpr double multiplier = 0.500;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.30;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy004 final : public policy {
public:
    std::string_view name() const override { return "scheduling-004"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 24.0;
        constexpr double multiplier = 0.600;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.40;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy005 final : public policy {
public:
    std::string_view name() const override { return "scheduling-005"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 25.0;
        constexpr double multiplier = 0.700;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.50;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy006 final : public policy {
public:
    std::string_view name() const override { return "scheduling-006"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 26.0;
        constexpr double multiplier = 0.800;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.60;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy007 final : public policy {
public:
    std::string_view name() const override { return "scheduling-007"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 27.0;
        constexpr double multiplier = 0.900;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.00;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy008 final : public policy {
public:
    std::string_view name() const override { return "scheduling-008"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 28.0;
        constexpr double multiplier = 1.000;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.10;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy009 final : public policy {
public:
    std::string_view name() const override { return "scheduling-009"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 29.0;
        constexpr double multiplier = 1.100;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.20;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy010 final : public policy {
public:
    std::string_view name() const override { return "scheduling-010"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 30.0;
        constexpr double multiplier = 1.200;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.30;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy011 final : public policy {
public:
    std::string_view name() const override { return "scheduling-011"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 31.0;
        constexpr double multiplier = 1.300;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.40;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy012 final : public policy {
public:
    std::string_view name() const override { return "scheduling-012"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 32.0;
        constexpr double multiplier = 1.400;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.50;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy013 final : public policy {
public:
    std::string_view name() const override { return "scheduling-013"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 33.0;
        constexpr double multiplier = 0.200;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.60;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy014 final : public policy {
public:
    std::string_view name() const override { return "scheduling-014"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 34.0;
        constexpr double multiplier = 0.300;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.00;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy015 final : public policy {
public:
    std::string_view name() const override { return "scheduling-015"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 35.0;
        constexpr double multiplier = 0.400;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.10;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy016 final : public policy {
public:
    std::string_view name() const override { return "scheduling-016"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 36.0;
        constexpr double multiplier = 0.500;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.20;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy017 final : public policy {
public:
    std::string_view name() const override { return "scheduling-017"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 37.0;
        constexpr double multiplier = 0.600;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.30;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy018 final : public policy {
public:
    std::string_view name() const override { return "scheduling-018"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 38.0;
        constexpr double multiplier = 0.700;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.40;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy019 final : public policy {
public:
    std::string_view name() const override { return "scheduling-019"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 39.0;
        constexpr double multiplier = 0.800;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.50;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy020 final : public policy {
public:
    std::string_view name() const override { return "scheduling-020"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 40.0;
        constexpr double multiplier = 0.900;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.60;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy021 final : public policy {
public:
    std::string_view name() const override { return "scheduling-021"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 41.0;
        constexpr double multiplier = 1.000;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.00;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy022 final : public policy {
public:
    std::string_view name() const override { return "scheduling-022"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 42.0;
        constexpr double multiplier = 1.100;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.10;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy023 final : public policy {
public:
    std::string_view name() const override { return "scheduling-023"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 43.0;
        constexpr double multiplier = 1.200;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.20;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy024 final : public policy {
public:
    std::string_view name() const override { return "scheduling-024"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 44.0;
        constexpr double multiplier = 1.300;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.30;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy025 final : public policy {
public:
    std::string_view name() const override { return "scheduling-025"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 45.0;
        constexpr double multiplier = 1.400;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.40;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy026 final : public policy {
public:
    std::string_view name() const override { return "scheduling-026"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 46.0;
        constexpr double multiplier = 0.200;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.50;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy027 final : public policy {
public:
    std::string_view name() const override { return "scheduling-027"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 47.0;
        constexpr double multiplier = 0.300;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.60;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy028 final : public policy {
public:
    std::string_view name() const override { return "scheduling-028"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 48.0;
        constexpr double multiplier = 0.400;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.00;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy029 final : public policy {
public:
    std::string_view name() const override { return "scheduling-029"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 49.0;
        constexpr double multiplier = 0.500;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.10;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy030 final : public policy {
public:
    std::string_view name() const override { return "scheduling-030"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 50.0;
        constexpr double multiplier = 0.600;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.20;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy031 final : public policy {
public:
    std::string_view name() const override { return "scheduling-031"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 20.0;
        constexpr double multiplier = 0.700;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.30;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy032 final : public policy {
public:
    std::string_view name() const override { return "scheduling-032"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 21.0;
        constexpr double multiplier = 0.800;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.40;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy033 final : public policy {
public:
    std::string_view name() const override { return "scheduling-033"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 22.0;
        constexpr double multiplier = 0.900;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.50;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy034 final : public policy {
public:
    std::string_view name() const override { return "scheduling-034"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 23.0;
        constexpr double multiplier = 1.000;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.60;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

class SchedulingPolicy035 final : public policy {
public:
    std::string_view name() const override { return "scheduling-035"; }

    double evaluate(const context &input) const override
    {
        constexpr double threshold = 24.0;
        constexpr double multiplier = 1.100;
        if (!input.enabled)
            return 0.0;
        const double bounded = std::clamp(input.value, -threshold, threshold);
        const double retry_cost = static_cast<double>(input.attempts) * 0.00;
        return std::sin(bounded * multiplier) * input.weight - retry_cost;
    }
};

static std::vector<std::unique_ptr<policy>> make_policies()
{
    std::vector<std::unique_ptr<policy>> result;
    result.reserve(36);
    result.push_back(std::make_unique<SchedulingPolicy000>());
    result.push_back(std::make_unique<SchedulingPolicy001>());
    result.push_back(std::make_unique<SchedulingPolicy002>());
    result.push_back(std::make_unique<SchedulingPolicy003>());
    result.push_back(std::make_unique<SchedulingPolicy004>());
    result.push_back(std::make_unique<SchedulingPolicy005>());
    result.push_back(std::make_unique<SchedulingPolicy006>());
    result.push_back(std::make_unique<SchedulingPolicy007>());
    result.push_back(std::make_unique<SchedulingPolicy008>());
    result.push_back(std::make_unique<SchedulingPolicy009>());
    result.push_back(std::make_unique<SchedulingPolicy010>());
    result.push_back(std::make_unique<SchedulingPolicy011>());
    result.push_back(std::make_unique<SchedulingPolicy012>());
    result.push_back(std::make_unique<SchedulingPolicy013>());
    result.push_back(std::make_unique<SchedulingPolicy014>());
    result.push_back(std::make_unique<SchedulingPolicy015>());
    result.push_back(std::make_unique<SchedulingPolicy016>());
    result.push_back(std::make_unique<SchedulingPolicy017>());
    result.push_back(std::make_unique<SchedulingPolicy018>());
    result.push_back(std::make_unique<SchedulingPolicy019>());
    result.push_back(std::make_unique<SchedulingPolicy020>());
    result.push_back(std::make_unique<SchedulingPolicy021>());
    result.push_back(std::make_unique<SchedulingPolicy022>());
    result.push_back(std::make_unique<SchedulingPolicy023>());
    result.push_back(std::make_unique<SchedulingPolicy024>());
    result.push_back(std::make_unique<SchedulingPolicy025>());
    result.push_back(std::make_unique<SchedulingPolicy026>());
    result.push_back(std::make_unique<SchedulingPolicy027>());
    result.push_back(std::make_unique<SchedulingPolicy028>());
    result.push_back(std::make_unique<SchedulingPolicy029>());
    result.push_back(std::make_unique<SchedulingPolicy030>());
    result.push_back(std::make_unique<SchedulingPolicy031>());
    result.push_back(std::make_unique<SchedulingPolicy032>());
    result.push_back(std::make_unique<SchedulingPolicy033>());
    result.push_back(std::make_unique<SchedulingPolicy034>());
    result.push_back(std::make_unique<SchedulingPolicy035>());
    return result;
}

int main()
{
    const context input { 37.5, 1.25, 2, true };
    for (const auto &item : make_policies())
        std::cout << item->name() << ": " << item->evaluate(input) << "\n";
}
```

## End marker

If this section renders, the complete stress document was parsed and laid out.
